BUILDING A WEBSITE

- Set the padding and margin to 0:
  *{
    padding:0;
    margin:0;
  }
  To avoid default padding and margin from the vs code.
-float css property:
- To call the html tag inside the div class/id:
  .className tagName{ ;}
  So all the tags outside the div tag are not affected.
